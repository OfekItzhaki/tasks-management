# Exclude everything by default
*

# Allow directories to be discovered
!*/

# Allow specific project folders and their contents
!mobile-app/**
!frontend-services/**

# Allow root config files
!package.json
!package-lock.json

# Standard exclusions (apply everywhere)
**/node_modules/
**/android/
**/ios/
**/.expo/
**/dist/
**/build/

# Explicitly exclude other monorepo apps just in case
todo-backend/
web-app/
mobile-build-isolated/
