{
  "name": "tasks-management",
  "private": true,
  "description": "Tasks Management monorepo",
  "scripts": {
    "start": "npm start --prefix web-app",
    "prepare": "husky"
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.4.2"
  },
  "lint-staged": {
    "web-app/src/**/*.{ts,tsx,css,json}": [
      "prettier --write",
      "eslint -c web-app/eslint.config.js --fix"
    ],
    "todo-backend/src/**/*.ts": [
      "prettier --write",
      "eslint -c todo-backend/eslint.config.mjs --fix"
    ]
  }
}