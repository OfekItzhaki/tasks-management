# Required
DATABASE_URL=postgresql://user:password@localhost:5432/tasks_db

# Optional
PORT=3000
NODE_ENV=development

# Production: comma-separated frontend URLs for CORS (e.g. https://app.example.com)
# ALLOWED_ORIGINS=

# Optional: Sentry DSN for error reporting (leave unset to disable)
# SENTRY_DSN=

# Email Configuration
# Option 1: Resend (recommended for production)
RESEND_API_KEY=

# Option 2: SMTP (alternative email provider)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-username
SMTP_PASSWORD=your-password
SMTP_FROM='"Horizon Flux" <no-reply@yourdomain.com>'

# Redis (for Background Jobs)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Cloudinary Storage (for Profile Pictures)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Cloudflare Turnstile (CAPTCHA)
# Get these keys from the Cloudflare Dashboard: https://dash.cloudflare.com/?to=/:account/turnstile
# 
# For development/testing, use the test secret key that always passes:
# TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA
#
# For production, create a Turnstile site in the Cloudflare Dashboard and use your secret key:
# TURNSTILE_SECRET_KEY=your-production-secret-key
#
# Leave unset to disable CAPTCHA verification (development mode)
TURNSTILE_SECRET_KEY=
